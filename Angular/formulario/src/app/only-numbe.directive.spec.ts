import { OnlyNumbeDirective } from './only-numbe.directive';

describe('OnlyNumbeDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumbeDirective();
    expect(directive).toBeTruthy();
  });
});
